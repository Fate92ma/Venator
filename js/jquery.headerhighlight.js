(function($){$.fn.headerhighlight=function(settings){function getCurrent(){var screenOffset=$(window).scrollTop();var $currentElement;$elementsHeader.each(function(){if(this.offsetTop<(screenOffset+offset)){$currentElement=$(this)}else{return!1}});if($currentElement===undefined)
$currentElement=$elementsHeader.eq(0);return $currentElement}
function setCurrent(){var $currentElement=getCurrent();var currentIdAttr=$currentElement.attr('id');$elementsChanging.removeClass(currentClass);$elementsChanging.filter('a[href="#'+currentIdAttr+'"]').addClass(currentClass)}
if(settings.currentClass===undefined)settings.currentClass='active';if(settings.offset===undefined)settings.offset=0;var $elementsHeader,$elementsChanging;var currentClass,offset;$elementsHeader=$(this);$elementsChanging=settings.elementsChanging;currentClass=settings.currentClass;offset=settings.offset;$(document).scroll(setCurrent);setCurrent()}})(jQuery)